# Must be sorted topologically, not alphabetically.
third_party = {
    'rustversion': subproject('rustversion').get_variable('dep'),
    'unicode_ident': subproject('unicode-ident').get_variable('dep'),
    'proc-macro2': subproject('proc-macro2').get_variable('dep'),
    'quote': subproject('quote').get_variable('dep'),
    'syn': subproject('syn').get_variable('dep'),
    'termcolor': subproject('termcolor').get_variable('dep'),
    'unicode_width': subproject('unicode-width').get_variable('dep'),
    'codespan-reporting': subproject('codespan-reporting').get_variable('dep'),
    'anstyle': subproject('anstyle').get_variable('dep'),
    'clap_lex': subproject('clap_lex').get_variable('dep'),
    'clap_builder': subproject('clap_builder').get_variable('dep'),
    'clap': subproject('clap').get_variable('dep'),
}
